<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.gralliams.dccehandbook"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DCCEHANDBOOK">
        <activity
            android:name=".SyllabusActivity"
            android:label="@string/title_activity_syllabus"
            android:theme="@style/Theme.DCCEHANDBOOK.NoActionBar"></activity>
        <activity android:name=".TagDisplayActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>